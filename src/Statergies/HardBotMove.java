package Statergies;

import Models.Board;
import Models.Move;
import Models.Player.Player;

public class HardBotMove implements BotMovingStats {
    @Override
    public Move makeMove(Board board, Player player) {
        return null;
    }
}
